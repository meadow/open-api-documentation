---
title: 'List all menu products'
openapi: 'GET /api/v1/menu'
---

<Warning>This endpoint needs to be retrieved using `daffodil.getmeadow.com` or `daffodil-staging.getmeadow.com` (staging sandbox) instead of the api subdomain.<br /><br />Example URL: `https://daffodil.getmeadow.com/api/v1/menu`</Warning>

<Note>This endpoint will return all products. No pagination is necessary</Note>
